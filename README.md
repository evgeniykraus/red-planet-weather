# 🔴 ARES ARCHIVE - Архив погодных данных Марса

Интерактивное веб-приложение для визуализации и анализа погодных данных Марса, собранных марсоходом NASA Curiosity с 2012 года. Представляет собой футуристичный терминал связи с марсоходом в стиле научной фантастики.

---

## 📋 О проекте

**ARES ARCHIVE** (Atmospheric Research & Environmental Statistics) — это иммерсивное приложение для исследования марсианской атмосферы с динамическим визуальным фоном, имитирующим атмосферные условия Красной планеты.

**Основные возможности:**
- 🎨 **Динамический Canvas-фон** с системой частиц, имитирующей пыльную атмосферу Марса
- 📊 **Статистика по месяцам** - детальный анализ температурных данных
- 🤖 **AI генерация изображений** - визуализация марсианских ландшафтов на основе погодных данных
- 🖼️ **Галерея** - коллекция сгенерированных изображений с интерпретацией
- 🎛️ **Интерактивная панель** - управление атмосферными параметрами (ветер, пыль, режим дня/ночи)
- 💻 **Терминальные загрузки** - футуристичный интерфейс в стиле связи с марсоходом

---

## 🚀 Быстрый старт

### Предварительные требования

- Docker и Docker Compose
- Git

### Установка и запуск

```bash
# 1. Клонируйте репозиторий
git clone https://github.com/evgeniykraus/red-planet-weather.git
cd red-planet-weather

# 2. Создайте файл окружения
cp .env.example .env

# 3. Установите PHP зависимости через Docker (без локального Composer)
docker run --rm \
    -u "$(id -u):$(id -g)" \
    -v "$(pwd):/var/www/html" \
    -w /var/www/html \
    laravelsail/php84-composer:latest \
    composer install --ignore-platform-reqs

# 4. Запустите Docker контейнеры через Sail
./vendor/bin/sail up -d

# 5. Сгенерируйте ключ приложения
./vendor/bin/sail artisan key:generate

# 6. Создайте символическую ссылку на storage
./vendor/bin/sail artisan storage:link

# 7. Установите Node зависимости
./vendor/bin/sail npm install

# 8. Выполните миграции и загрузите данные
./vendor/bin/sail artisan migrate:fresh
./vendor/bin/sail artisan db:seed --class=MarsWeatherSeeder

# 9. Соберите frontend
./vendor/bin/sail npm run build
```

### Доступ к приложению

Откройте браузер и перейдите на **http://localhost**

### Остановка приложения

```bash
./vendor/bin/sail down
```

---

## 💡 Как использовать

### Главная страница (ARES ARCHIVE)

1. **Выберите месяц** - используйте селектор для выбора марсианского месяца (1-12)
2. **Изучите статистику** - просмотрите температурные данные, сезонную информацию
3. **Сгенерируйте изображение** - создайте AI-визуализацию марсианского ландшафта
4. **Управляйте атмосферой** - используйте боковую панель для изменения параметров фона:
   - Скорость ветра (0-100 км/ч)
   - Плотность пыли (Чистая → Норма → Пыльная бури)
   - Режим времени суток (День / Буря / Ночь)
   - Кнопка "Случайный день" для рандомизации

### Галерея

- Просмотрите все сгенерированные AI изображения
- Фильтруйте по марсианским месяцам
- Кликните на изображение для полноэкранного просмотра с интерпретацией

**Что означают марсианские месяцы?**

Марсианский год ≈ 687 земных дней и разделен на 12 месяцев по 30° солнечной долготы (Ls):
- **Месяцы 1-3:** Весна (северное полушарие) 🌱
- **Месяцы 4-6:** Лето (северное полушарие) ☀️
- **Месяцы 7-9:** Осень (северное полушарие) 🍂
- **Месяцы 10-12:** Зима (северное полушарие) ❄️

---

## 🛠️ Стек технологий

**Backend:**
- **Laravel 12** - PHP фреймворк
- **PostgreSQL** - база данных
- **Repository + Service Layer** - архитектурный паттерн
- **Laravel Sail** - Docker окружение
- **Inertia.js v2** - современный monolith без API

**Frontend:**
- **Vue 3** + **TypeScript** - реактивный UI
- **Composition API** - современный подход к Vue
- **Tailwind CSS v4** - utility-first CSS фреймворк
- **Lucide Icons** - иконки
- **HTML5 Canvas** - динамический фон с частицами
- **Marked + DOMPurify** - безопасный рендеринг Markdown

**AI & Генерация:**
- **BotHub API** - доступ к AI моделям (Gemini 2.5 Flash Image)
- **Gemini 2.5 Flash Image** - генерация марсианских ландшафтов
- **Prompt Engineering** - специализированные промпты для реалистичных визуализаций

**DevOps:**
- **Docker & Docker Compose** - контейнеризация
- **Vite** - сборщик frontend
- **Laravel Pint** - форматирование PHP кода
- **PHPUnit** - тестирование

---

## ⚙️ Конфигурация

### Переменные окружения

После создания файла `.env` обязательно настройте следующие параметры:

```env
# База данных (настроено для Sail по умолчанию)
DB_CONNECTION=pgsql
DB_HOST=pgsql
DB_PORT=5432
DB_DATABASE=red_planet_weather
DB_USERNAME=sail
DB_PASSWORD=password

# BotHub AI (для генерации изображений через Gemini)
BOTHUB_BASE_URL=https://bothub.chat/api/v2/openai/v1
BOTHUB_API_KEY=your_bothub_api_key_here
BOTHUB_MODEL=gemini-2.5-flash-image

### Получение BotHub API ключа

1. Зарегистрируйтесь на [BotHub.chat](https://bothub.chat)
2. Перейдите в раздел API Keys
3. Создайте новый API ключ
4. Добавьте его в `.env` как `BOTHUB_API_KEY`

**Что такое BotHub?**  
BotHub предоставляет доступ к различным AI моделям, включая Gemini 2.5 Flash Image, которая используется для генерации марсианских ландшафтов.

**Примечание:** Без API ключа функция генерации изображений работать не будет, но остальной функционал приложения доступен.

---

## 🧪 Тестирование

```bash
# Запустить все тесты
./vendor/bin/sail artisan test

# Запустить конкретный тест
./vendor/bin/sail artisan test --filter=test_index_page_loads

# Запустить тесты с покрытием
./vendor/bin/sail artisan test --coverage
```

---

## 🔄 Обновление данных

Для получения актуальных данных о погоде с API:

```bash
./vendor/bin/sail artisan mars:fetch-weather-csv
```

Команда загружает последние данные с Apollorion API и сохраняет их в CSV файл.

---

## 🛠️ Разработка

### Запуск в режиме разработки

```bash
# Запустить Vite dev server для hot reload
./vendor/bin/sail npm run dev

# В другом терминале запустите контейнеры
./vendor/bin/sail up
```

### Полезные команды

```bash
# Форматирование PHP кода
./vendor/bin/sail bin pint

# Логи приложения
./vendor/bin/sail logs -f

# Доступ к контейнеру
./vendor/bin/sail shell

# Доступ к базе данных
./vendor/bin/sail psql

# Очистка кеша
./vendor/bin/sail artisan cache:clear
./vendor/bin/sail artisan config:clear
./vendor/bin/sail artisan route:clear
```

---

## 🐛 Troubleshooting

### Проблема: "Permission denied" при запуске Docker команд

```bash
# Дайте права на выполнение для sail
chmod +x vendor/bin/sail
```

### Проблема: Порт 80 уже занят

Измените порт в `.env`:
```env
APP_PORT=8080
```

Затем перезапустите контейнеры:
```bash
./vendor/bin/sail down
./vendor/bin/sail up -d
```

### Проблема: Frontend не обновляется

```bash
# Очистите кеш и пересоберите
./vendor/bin/sail npm run build
# Или запустите dev server
./vendor/bin/sail npm run dev
```

### Проблема: Ошибка "Vite manifest not found"

```bash
# Убедитесь, что frontend собран
./vendor/bin/sail npm install
./vendor/bin/sail npm run build
```

---

## 📚 Источник данных

Данные получены через **Apollorion MAAS API** ([api.maas2.apollorion.com](https://api.maas2.apollorion.com)) — архив погодных наблюдений ровера NASA Curiosity с 6 августа 2012 года (Sol 0).
